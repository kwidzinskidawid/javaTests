Given a messenger
When server is inf.ug.edu.pl and message is "some message"
Then it should return code status 0 or 1

When server is inf.ug.edu.eu and message is "some message"
Then

When server is inf.ug.edu.pl and message is "ab"
Then

When server is inf.ug.edu.eu and message is "ab"
Then